## Current agent-building practice (generated - do not hand-edit)
> [digest bounded: 11 of 11 bullets shown, 0 truncated, 0 dropped, as of 2026-08-16]
- **[PRC-007 / verification-gates / guardrail / value 9.5 / conf 5 / established]** Decide whether a stage succeeded by checking for a concrete artifact it was required to produce, never by trusting its exit code or the agent's own claim of success. _Check:_ Does the orchestrator gate on os.path.exists of a declared artifact rather than on the return code? If it reads exit codes or parses agent prose, it is trusting the wrong signal.
- **[PRC-011 / sandbox-safety / guardrail / value 9.5 / conf 3 / established]** Let agents try, fail, and retry freely inside a constrained environment, but require a human in the loop before anything reaches production; autonomy should come from the sandbox, not from trust. _Check:_ Can the loop cause an externally-visible effect without a human approving it? If yes, the gate is missing.
- **[PRC-003 / context-engineering / pattern / value 9.0 / conf 3 / consolidating]** Curate the context window as a finite attention budget: find the smallest set of high-signal tokens that produce the desired behavior, rather than stuffing in everything that might be relevant, because recall degrades as the window fills (context rot). _Check:_ Is anything in the current context there because it MIGHT be relevant rather than because it is high-signal for the next step? That is budget you are wasting.
- **[PRC-006 / loop-architecture / pattern / value 9.0 / conf 3 / consolidating]** In an autonomous build loop, do exactly one task per iteration and spawn a fresh instance each time, keeping per-iteration context as small as possible, because output quality falls as the window fills. _Check:_ Does each iteration start a fresh instance and complete one scoped task, with state carried on disk rather than in a growing context? If iterations share a ballooning window, this is violated.
- **[PRC-008 / verification-gates / pattern / value 9.0 / conf 5 / consolidating]** Under any hard wall-clock cap, have each step emit a schema-valid minimal complete output within the first seconds and then improve it in place, so a kill degrades quality instead of destroying the step. _Check:_ If a step is killed at a random time, does a valid (if rough) output already exist on disk? If not, it is not checkpoint-first.
- **[PRC-009 / verification-gates / guardrail / value 9.0 / conf 5 / consolidating]** When a machine-parsed verdict decides ship-versus-revert, never let a missing token default to the destructive outcome; write the verdict early on decisive evidence and budget verification so a timeout cannot erase it. _Check:_ If the verdict stage is killed before writing anything, what happens? If the answer is 'the work is reverted', the default is unsafe.
- **[PRC-010 / feedback-loops / pattern / value 9.0 / conf 5 / established]** Before expecting an agent to work autonomously on a codebase, make its feedback loop fast and reliable (quick tests, good error messages), because an agent is only as good as the signal it gets and a slow suite starves it. _Check:_ How many times per hour can the agent get a full pass/fail signal? If it is single digits, the loop is starved.
- **[PRC-001 / loop-architecture / pattern / value 8.0 / conf 3 / established]** Reach for the simplest design first (a single call, a fixed workflow) and add autonomous-agent complexity only when a simpler solution demonstrably falls short, because agency trades latency and cost for flexibility you may not need. _Check:_ Can you point to a measured failure of a simpler workflow that justifies the current agentic complexity? If not, it is unjustified.
- **[PRC-004 / memory-state / pattern / value 8.0 / conf 3 / consolidating]** On tasks that exceed one context window, have the agent write structured notes to durable storage (a NOTES.md, a to-do list, a ledger) and read them back after a reset, so progress and decisions survive compaction. _Check:_ After a simulated context reset, can the agent resume correctly from its own notes alone? If not, its memory is not durable.
- **[PRC-002 / tool-interface / pattern / value 7.5 / conf 3 / established]** Treat an agent as an LLM calling tools in a loop against environmental feedback, and invest in the agent-computer interface (clear tool docs, unambiguous parameters, poka-yoke arguments) as heavily as the prompt itself. _Check:_ For each tool, can a new engineer say from the description alone exactly when to use it and not confuse it with another? If not, the tool set is a failure source.
- **[PRC-005 / multi-agent / pattern / value 7.0 / conf 3 / consolidating]** For complex research or exploration, delegate deep work to sub-agents with clean context windows and have each return only a condensed summary (roughly 1,000-2,000 tokens), so the lead agent synthesizes results without inheriting the detailed search context. _Check:_ Does each sub-agent return a bounded summary rather than its full transcript? If the lead agent sees raw sub-traces, isolation is not happening.
